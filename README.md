# Tabata
My own tabata timer. Run using `python view.py` or `python proper_view.py`.
view.py uses multiple windows for settings, the timer and a workout summary. 
proper_view uses one window for all three views. 